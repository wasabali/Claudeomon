{
  "name": "cloud-quest",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --exclude tests/stress/**",
    "test:stress": "vitest run tests/stress/",
    "test:watch": "vitest --exclude tests/stress/**",
    "gen:maps": "node scripts/generate-maps.js",
    "gen:tilesets": "node scripts/generate-tilesets.js",
    "assets:stubs": "node scripts/stub-sprites.js",
    "assets:install": "node scripts/install-ninja-assets.js"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
